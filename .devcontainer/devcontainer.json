{
    "name": "Windows Dev with VNC",
    "dockerfile": "Dockerfile",

    // Forward both VNC and noVNC ports
    "forwardPorts": [5901, 8080],

    // You might want an extension to open web URLs easily within Codespaces
    "extensions": [
        "ms-vscode.vscode-github-browser-extension" 
    ]
}
